# v2board







```shell
yum -y install git wget && git clone https://github.com/ipmartnetwork/v2board.git /usr/local/src/v2board_install && cd /usr/local/src/v2board && chmod +x v2board.sh && ./v2board.sh
```
